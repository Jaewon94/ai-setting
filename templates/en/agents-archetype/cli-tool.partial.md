## CLI Agent Rules
- Treat help text, exit codes, and stdout/stderr separation as part of the public interface.
- When flags or commands change, update examples, docs, and tests together.
- Keep human-readable output separate from machine-readable output such as `--json`.
