## CLI 규칙
- 모든 명령은 --help를 지원하고, 사용법이 명확해야 한다
- 종료 코드를 의미 있게 사용 (0=성공, 1=일반 에러, 2=사용법 에러)
- stdin/stdout/stderr를 올바르게 구분 (결과는 stdout, 진행/에러는 stderr)
- 긴 작업은 진행 표시 (프로그레스 바, 스피너 등)
- 설정은 CLI 인자 > 환경변수 > 설정 파일 우선순위
- 파이프 친화적: 사람이 읽는 출력과 기계가 읽는 출력(--json 등) 분리
- 테스트: CLI 인수 파싱 테스트 + 주요 명령 통합 테스트
