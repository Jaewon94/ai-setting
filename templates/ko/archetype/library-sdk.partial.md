## 라이브러리/SDK 규칙
- 공개 API는 최소한으로 유지 — 내부 구현은 노출하지 않는다
- 모든 공개 함수/클래스에 타입 정의와 docstring/JSDoc 필수
- 하위 호환성을 의식한다 — breaking change는 major 버전에서만
- 의존성은 최소한으로 — 외부 의존성 하나 추가가 사용자에게 전이됨
- 에러는 명확한 커스텀 예외/에러 타입으로 — 내부 에러를 그대로 전파하지 않는다
- 예제 코드와 README는 항상 최신 API를 반영
- 테스트: 공개 API 100% 커버리지 + 하위 호환성 테스트
